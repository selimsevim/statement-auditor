# Statement Auditor — environment configuration
#
# Copy this file to `.env` (which is gitignored) and fill in your key, or
# simply export ANTHROPIC_API_KEY in your shell. Real environment variables
# always take precedence over values in `.env`.

ANTHROPIC_API_KEY=sk-ant-...
